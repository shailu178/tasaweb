# Vercel Postgres — get from Vercel dashboard → Storage → Create Database
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NO_SSL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# Vercel Blob — get from Vercel dashboard → Storage → Create Blob Store
BLOB_READ_WRITE_TOKEN=
